// BSLLS:ExportVariables-off
// BSLLS:MissingVariablesDescription-off
// Стаб сеанса для СервисКластера-тестов.

Перем Свойства_ Экспорт;
Перем Завершен Экспорт;
Перем БроситьПриЗавершении Экспорт;
Перем ЗавершитсяСПопытки Экспорт; // "зависший" сеанс: завершается только с N-го вызова Завершить()
Перем ВызововЗавершения Экспорт;

Процедура ПриСозданииОбъекта()
	Свойства_ = Новый Соответствие;
	Свойства_.Вставить("session-id", "1");
	Свойства_.Вставить("app-id", "Designer");
	Завершен = Ложь;
	БроситьПриЗавершении = Ложь;
	ЗавершитсяСПопытки = 1;
	ВызововЗавершения = 0;
КонецПроцедуры

Функция СоСвойством(Ключ, Значение) Экспорт
	Свойства_.Вставить(Ключ, Значение);
	Возврат ЭтотОбъект;
КонецФункции

Функция Зависший(Знач ПопытокДоЗавершения) Экспорт
	ЗавершитсяСПопытки = ПопытокДоЗавершения;
	Возврат ЭтотОбъект;
КонецФункции

Функция Получить(Ключ) Экспорт
	Возврат Свойства_[Ключ];
КонецФункции

Процедура Завершить() Экспорт
	Если БроситьПриЗавершении Тогда
		ВызватьИсключение "сеанс невозможно завершить";
	КонецЕсли;
	ВызововЗавершения = ВызововЗавершения + 1;
	Если ВызововЗавершения >= ЗавершитсяСПопытки Тогда
		Завершен = Истина;
	КонецЕсли;
КонецПроцедуры
