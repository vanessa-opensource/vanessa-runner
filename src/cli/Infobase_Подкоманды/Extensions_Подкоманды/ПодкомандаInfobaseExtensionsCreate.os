// BSLLS:LineLength-off
// BSLLS:NumberOfValuesInStructureConstructor-off
// BSLLS:MissingVariablesDescription-off Поля &Опция/&Аргумент/&НаборОпций - описания заданы в самой аннотации.
&Лог("vrunner")
Перем Лог;

&Пластилин
Перем СервисРасширений;

&Пластилин
Перем СервисЗаполненияПараметров;

&НаборОпций("НаборОпцийПодключения")
Перем ОпцииПодключения;

&НаборОпций("НаборОпцийПлатформы")
Перем ОпцииПлатформы;

&НаборОпций("НаборОпцийСУБД")
Перем ОпцииСУБД;

&Аргумент(Имя = "NAME", Описание = "Имя создаваемого расширения")
&ТСтрока
Перем ИмяРасширения;

&Опция(Имя = "overwrite", Описание = "Пересоздать расширение, если оно уже существует (иначе - ошибка)")
&Флаговый
Перем Перезаписывать;

&ПодкомандаПриложения(Имя = "create", Родитель = "ПодкомандаInfobaseExtensions",
		Описание = "Создать пустое расширение конфигурации с заданным именем")
&ДоступноВMCP
&MCPРазрушающая
Процедура ПриСозданииОбъекта()
КонецПроцедуры

&ВыполнениеКоманды
Процедура ВыполнитьДействие() Экспорт

	СервисЗаполненияПараметров.Заполнить(ЭтотОбъект);

	Если НЕ ЗначениеЗаполнено(ИмяРасширения) Тогда
		ВызватьИсключение "Не задано имя расширения (аргумент NAME)";
	КонецЕсли;

	Лог.Отладка("Начало выполнения команды infobase extensions create: %1", ИмяРасширения);

	СервисРасширений.СоздатьПустоеРасширение(
		СформироватьПараметрыПодключения(),
		ИмяРасширения,
		Перезаписывать = Истина,
		СформироватьПараметрыПлатформы());

КонецПроцедуры

#Область СлужебныеПроцедурыИФункции

Функция СформироватьПараметрыПлатформы()

	Возврат Новый Структура(
		"ВерсияПлатформы, КодРазрешения, Язык, Локаль",
		ОпцииПлатформы.ВерсияПлатформы,
		ОпцииПлатформы.КодРазрешения,
		ОпцииПлатформы.Язык,
		ОпцииПлатформы.Локаль);

КонецФункции

Функция СформироватьПараметрыПодключения()

	Возврат ОпцииПодключения.СформироватьПараметрыПодключения(ОпцииСУБД);

КонецФункции

#КонецОбласти
